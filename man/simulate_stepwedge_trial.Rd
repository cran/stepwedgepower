% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/power.R
\name{simulate_stepwedge_trial}
\alias{simulate_stepwedge_trial}
\title{Simulate one stepped-wedge trial dataset}
\usage{
simulate_stepwedge_trial(
  treatment_or = 1.5,
  n_clusters_per_sequence = c(40, 40, 40, 40),
  sequence_names = paste0("Sequence ", seq_along(n_clusters_per_sequence)),
  cluster_sd = 1.21,
  icc = NULL,
  baseline_probs = c(0.06, 0.04, 0.03, 0.02),
  n_per_cluster_period = 20,
  n_periods = length(n_clusters_per_sequence) + 1L,
  seed = NULL,
  effect_size_or = NULL,
  n_providers_per_specialty = NULL,
  specialty_names = NULL,
  tau_provider = NULL,
  base_probs = NULL,
  pts_per_step = NULL,
  n_steps = NULL
)
}
\arguments{
\item{treatment_or}{Odds ratio for treatment under the data-generating model.}

\item{n_clusters_per_sequence}{Integer vector giving the number of clusters in
each sequence.}

\item{sequence_names}{Labels for the stepped-wedge sequences.}

\item{cluster_sd}{Standard deviation of the cluster random intercept. Supply
either this argument or \code{icc}.}

\item{icc}{Optional latent-scale intraclass correlation coefficient for a
logistic random-intercept model.}

\item{baseline_probs}{Baseline outcome probabilities for each sequence.}

\item{n_per_cluster_period}{Number of observations per cluster-period.}

\item{n_periods}{Number of study periods. The default is one baseline period
plus one crossover period per sequence.}

\item{seed}{Optional random seed.}

\item{effect_size_or}{Deprecated alias for \code{treatment_or}.}

\item{n_providers_per_specialty}{Deprecated alias for
\code{n_clusters_per_sequence}.}

\item{specialty_names}{Deprecated alias for \code{sequence_names}.}

\item{tau_provider}{Deprecated alias for \code{cluster_sd}.}

\item{base_probs}{Deprecated alias for \code{baseline_probs}.}

\item{pts_per_step}{Deprecated alias for \code{n_per_cluster_period}.}

\item{n_steps}{Deprecated alias for \code{n_periods}.}
}
\value{
A data frame with one row per cluster-period combination. Generic
column names are supplied together with legacy aliases for compatibility.
}
\description{
Generates aggregated cluster-by-period binomial data for a stepped-wedge
design in which cluster sequences cross over sequentially.
}
\examples{
sim <- simulate_stepwedge_trial(
  treatment_or = 1.5,
  n_clusters_per_sequence = c(10, 10, 10, 10),
  baseline_probs = rep(0.05, 4),
  icc = 0.05,
  n_per_cluster_period = 20,
  seed = 1
)
head(sim[, c("cluster_id", "sequence", "period", "intervention", "n", "events")])
}
